# 公司月度成本流向与净利润分析系统 · 开发记录

> 本文档整理自与用户的完整对话，记录「Cost flow」项目从需求设计、建站、自动更新到多轮流程图迭代的全过程。

---

## 一、项目概述

**目标**：构建一个单文件 Web 程序，按月录入 5 条产品线的收入与成本，自动核算盈利状况，并以 SVG「主水管流程图」直观呈现成本流向与净利润的形成。

**核心核算逻辑**
- 毛利润 = 营业收入 − 直接成本
- 间接成本按**各产品线直接成本占比**分摊（最初为按收入占比，后改为按直接成本占比）
- 税前净利润 = 毛利润 − 分摊间接成本
- 净利润 = 税前净利润 × (1 − 税率)（亏损月份不计提所得税，保守口径）
- 经营现金流 ≈ 净利润 + 折旧 − 应收账款增加 − 可用库存增加

**5 条产品线**
| 产品线 | 类型 | 直接成本字段 |
|---|---|---|
| DAISO 填料 | 代理 | 直接采购成本、包装运输成本 |
| 自营填料 | 自营 | 生产人力、材料、生产水电煤租赁、设备采购租赁、包装运输 |
| COSMOSIL 柱 | 代理 | 直接采购成本、包装运输成本 |
| 自营色谱柱 | 自营 | 生产人力、材料、生产水电煤租赁、设备采购租赁、包装运输 |
| 其他产品 | 自营 | 生产人力、材料、生产水电煤租赁、设备采购租赁、包装运输 |

---

## 二、需求演变时间线

| # | 用户诉求 | 关键决策 / 结果 |
|---|---|---|
| 1 | 创建程序：5 条产品线月度录入，直接/间接成本分层核算，逐线展示毛利润/税前净利/净利润，SVG 主水管流程图 | 交付单文件 HTML，localStorage 持久化，已本地运行 |
| 2 | 间接成本分摊改为「按直接成本占比」；授权用 GitHub 建站并可视化 | 复用已有 fx-tracker 仓库中的 PAT 令牌建库推送 |
| 3 | 若建不了站，先推送到 GitHub | 已推送 |
| 4 | 给 Cost flow 接上自动更新 | 新增 server.py 发布接口 + data/costflow.json + 每日自动化任务 |
| 5 | 界面区块顺序调整：原 6→1、4→2、5→3，原 1/2/3→4/5/6 | 仅移动 DOM 顺序 + 改编号 |
| 6 | 流程图 5 点细节：直接成本左移树枝、标题动态化、间接成本一级分支、税款右侧收窄、主水管加粗 | 全部落地 |
| 7 | 毛利润、税前净利润放入菱形框；税款从主水管剔除放下游净利润前 | 实现后用户认为「太丑」 |
| 8 | ①菱形移出主水管放右侧 ②税款恢复主水管下方分支 ③主水管标题两行 ④换协调高级低饱和配色 | 莫兰迪系配色落地 |
| 9 | ①毛利润挪到直接成本与间接成本中间 ②税前净利润移回原毛利润位置 ③营业收入与数值同放第二行 | 本会话第一轮改动 |
| 10 | 毛利润与税前净利润菱形保持一致大小；相应拉长主水管；税款框右移给前置分支留白 | 本会话第二轮改动 |

---

## 三、技术方案

| 维度 | 方案 |
|---|---|
| 前端 | 单文件 `index.html`（内嵌 HTML + CSS + JS，无构建步骤） |
| 持久化 | `localStorage`（key `costflow_<月份>` + 索引 `costflow_index`）；支持导出/导入 JSON、月份切换 |
| 可视化 | SVG 动态渲染（`renderFlow()` 生成 `<svg>` 注入 `#flowChart`）；`compute()` 核算引擎 |
| 发布 | 本地 `server.py` 提供静态页 + `POST /api/publish` 写 `data/costflow.json` |
| 建站 | GitHub Pages（main 分支根目录）+ `.nojekyll` |
| 凭证 | 复用 fx-tracker 仓库 `.git/config` 中的 PAT，经 `git -c url...insteadOf` 传入，**不写入** 本仓库配置 |
| 自动更新 | WorkBuddy 自动化（每日 09:30，提取令牌 → pull → commit「自动更新:日期」→ push） |
| 坐标校验 | Node 沙箱跑真实 `renderFlow` 生成 SVG，Python 解析 rect/polygon 坐标查越界与重叠 |

---

## 四、文件结构

```
Cost flow/
├── index.html          # 核心单文件（录入 / 计算 / 表格 / SVG 流程图）
├── server.py           # 本地静态服务 + /api/publish 发布接口
├── data/costflow.json  # 已发布数据（仓库与线上站一致）
├── .nojekyll           # 禁用 Jekyll
└── .workbuddy/memory/  # 项目日志
```

---

## 五、流程图迭代细节（第 9–10 轮）

### 第 9 轮：菱形重排 + 标题第二行
- **毛利润菱形** 移到「直接成本(220) ↔ 间接成本(600)」之间的顶部空隙（x≈410，恰好卡进两节点间隙）；**税前净利润菱形** 移到原毛利润右侧位置（x≈775）。
- 主水管标题：第一行仅显示「对象 + 月份」（如「公司合计 2026年7月」），第二行改为「营业收入 ¥xxx」。
- 校验：修复产前税前净利润与间接成本节点 5px 条带重叠（右移修掉）。

### 第 10 轮：菱形同尺寸 + 主水管拉长 + 税款右移
- 两菱形统一 `DIA_RW=64, DIA_RH=42`（128×84）同尺寸。
- 主水管 `pipeW 860→1000`，画布 `1040×700 → 1120×710`。
- 分支间距拉开：`directX 200→220`、`indirectX 500→600`、`taxX 800→900`（税款框右移给前置分支留白）。
- 末端净利池汇入点 `netDropX 900→980`，避开右移后的税款框（845–955）。

**最终布局坐标（查看对象=公司合计视图）**
| 元素 | x 范围 | y 范围 | 说明 |
|---|---|---|---|
| 主水管（营业收入） | 40–1040 | 40–100 | 加粗渐变青灰 |
| 直接成本节点 | 115–325 | 156–206 | 左侧分支 + 树枝状明细 |
| 间接成本节点 | 495–705 | 156–206 | 中间分支 + 研发/销售/管理一级分支 |
| 税款节点 | 845–955 | 150–192 | 右侧短分支 |
| 毛利润菱形 | 346–474 | 118–202 | 直接/间接成本中间 |
| 税前净利润菱形 | 711–839 | 118–202 | 间接成本右侧 |
| 净利润池 | 610–920 | 540–602 | 末端汇入 |
| 可用库存 / 经营现金流 | 610–760 / 775–925 | 624–670 | 子池 |

---

## 六、当前状态与入口

- **线上站**：https://caiclhy0806.github.io/cost-flow-analysis/
- **仓库**：https://github.com/caiclhy0806/cost-flow-analysis
- **本地预览**：http://127.0.0.1:8765/index.html（server.py 后台运行）
- **最新 commit**：`80a39e6`（流程图优化：毛利润/税前净利润菱形同尺寸，主水管拉长，税款框右移留白）

### 待办建议（用户未确认）
1. 清理 fx-tracker 仓库中明文存储的 PAT（建议去 GitHub revoke 轮换）。
2. 评估每日自动化的提交频率是否合适。

---

*文档生成于 2026-07-12，整理自本次完整对话。*
